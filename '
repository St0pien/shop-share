import { Dialog, DialogHeader, DialogTitle } from '@/components/ui/dialog';
import { type UserInfo } from 'os';

export function KickUserDialog({ user }: { user: UserInfo }) {
  return <Dialog>
  <DialogHeader>
      <DialogTitle>Kick out user</DialogTitle>
    </DialogHeader>
  </Dialog>
}
