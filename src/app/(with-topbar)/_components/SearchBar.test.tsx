import { describe, test } from 'vitest';
import { render } from '@testing-library/react';


describe('SearchBar', () => {
  test('should reflect input into URL', () => {
    render(<h1>hello there</h1>);

    await u
  });
});
